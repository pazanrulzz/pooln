generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models land in Phase 1+ (users, groups, expenses, ...).
// Phase 0 only wires up the Prisma <-> Postgres connection.
